{
  "id": "time",
  "name": "time",
  "version": "1.0.0",
  "input_schema": {
    "type": "object",
    "properties": {
      "echo": {
        "type": "string"
      }
    },
    "additionalProperties": false
  },
  "nodes": [
    {
      "id": "make_time",
      "type": "function",
      "name": "make_time",
      "code": "from datetime import datetime, timezone\n\nnow = datetime.now(timezone.utc).isoformat()\n\n# Normalize to Z-terminated if needed\nif not now.endswith('Z'):\n    if '+' in now or now.endswith('00:00'):\n        now = now.replace('+00:00', 'Z')\n    elif now.endswith('0000'):\n        now = now[:-4] + 'Z'\n    else:\n        now = now + 'Z'\n\noutput = { 'time': now }\n\nif 'echo' in inputs and isinstance(inputs['echo'], str):\n    output['echo'] = inputs['echo']\n\nreturn output"
    }
  ],
  "edges": [],
  "output": {
    "from": "make_time"
  }
}
